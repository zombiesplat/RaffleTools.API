language: php
php:
  - '5.6'
  - '7.0'

before_script:
  - composer self-update

install: travis_retry composer install --no-interaction --prefer-source

script: phpunit --coverage-clover builds/logs/clover.xml
